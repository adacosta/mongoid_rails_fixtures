require File.dirname(__FILE__) + "/lib/mongoid_rails_fixtures/test/config"

namespace :test do
  namespace :mongoid do
    desc "Test mongoid rails migrations"
    task :fixtures do
      require 'mongoid_rails_fixtures/test/fixture_test'
    end
  end
end